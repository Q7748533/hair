# HairToolsReview — Project Memory

> **Last updated:** 2026-05-10 (+ CSS cache debugging + branch cleanup + orphan commit rescue)
> **Purpose:** Session continuity for AI assistants working on this project
> **Read this first** before making any changes

---

## 1. Project Overview

**Brand:** HairToolsReview
**Business Entity:** Palm Media Technology, United States
**Domain:** hairtoolslab.com (deployed on Vercel)
**Repo:** Q7748533/hair (branch: main)
**Type:** Amazon affiliate review site (Amazon Associates Program)
**Content Focus:** Hair styling tools — currently steam straighteners + hair dryers

---

## 2. Tech Stack & Deployment

- **Static HTML + CSS** (no JS framework)
- **Shared stylesheet:** `assets/styles.css` (source, 20KB) + `assets/styles.min.css` (prod, 14KB, -30%)
  - All HTML references `styles.min.css`. Never edit `.min.css` directly — edit `.css` and re-minify.
- **Deploy:** Vercel, auto-deploy from `main` branch
- **Config:** `vercel.json` → `cleanUrls: true`, `trailingSlash: false`, cache headers, /landing redirects
- **Analytics:** GA4, ID `G-HFT4XZ2YB2`, Consent Mode v2 (default deny until user opts in)
  - gtag.js library is **deferred**: consent defaults fire immediately, but the 70KB library only loads on `requestIdleCallback` (timeout 4s) or first user interaction (scroll/click/keydown/touchstart/mousemove).
- **Images:** Direct Amazon CDN URLs (no local hosting), hotlinked for og:image too
- **Perf hints:** preconnect + dns-prefetch to `m.media-amazon.com` on every page. Review pages additionally preload the hero `<img>` with `fetchpriority="high"` and `decoding="async"`.
- **Internal docs NOT shipped to prod:** `.vercelignore` excludes `.kiro-memory.md`, `soul.ilang`, `project-rules.md`, `ilang-example.md`, `data/`

---

## 3. Current File Structure

```
hairtoolslab.com/
├── index.html                                      (homepage/brand portal, uses .link-card + .home-hero classes)
├── steam-straighteners.html                        (category hub, CollectionPage schema)
│   ├── wavytalk-pro-steam-straightener-review.html (4.5★, $67, Article+Product+FAQPage+Breadcrumb schema)
│   ├── wavytalk-steam-straightener-4c-hair.html    (4C hair deep-dive, Article+Breadcrumb schema, 176 reviews)
│   ├── best-steam-straighteners-2026.html          (Top 5, Article+ItemList+Breadcrumb schema)
│   ├── steam-straightener-vs-flat-iron.html        (comparison, Article+Breadcrumb schema)
│   └── how-to-use-steam-straightener.html          (how-to, HowTo+Breadcrumb schema)
├── hair-dryers.html                                (category hub, CollectionPage schema, ItemList has 3 items)
│   ├── wavytalk-ionic-hair-dryer-review.html       (4.4★, $33, Article+Product+FAQPage+Breadcrumb schema)
│   ├── best-ionic-hair-dryers-2026.html            (NEW Top 5, Article+ItemList+Breadcrumb schema)
│   └── how-to-use-a-diffuser.html                  (NEW how-to, HowTo+Breadcrumb schema, 7 steps)
├── about.html                                      (E-E-A-T, Organization schema, #process anchor)
├── contact.html                                    (Formspree-ready form w/ mailto fallback, honeypot, async)
├── privacy-policy.html                             (updated 2026-01-01)
├── terms.html                                      (updated 2026-01-01)
├── 404.html                                        (noindex, on-brand, uses .error-* classes)
└── offer.html                                      (DO NOT TOUCH — owner-managed, noindex)

assets/
├── styles.css                                      (source — edit this, ~22KB after a11y+link-card additions)
├── styles.min.css                                  (production — referenced by all HTML, ~15KB gzipped further by Vercel)
├── favicon.svg
└── og-default.svg                                  (1200x630 social preview, used on about/contact/privacy/terms/404)

Root config:
├── robots.txt                                      (explicit Disallow for internal docs + .md/.ilang)
├── sitemap.xml                                     (15 URLs, accurate per-page lastmod, priority 0.3–1.0)
├── vercel.json                                     (cleanUrls, cache headers, /landing 301s)
├── .vercelignore                                   (hides internal docs from prod)
└── .gitignore

Internal (never deployed, in .vercelignore):
├── .kiro-memory.md                                 (this file)
├── soul.ilang                                      (I-Lang v3.0 behavioral DNA)
├── project-rules.md                                (legacy I-Lang v2.0 rules)
├── ilang-example.md                                (I-Lang syntax example)
└── data/                                           (source data, not deployed)
```

**Total public pages: 16** (plus offer.html untouched = 17 HTML files on disk)

---

## 4. Products Currently Covered

### Product 1: Wavytalk Pro Steamline 3-in-1 Steam Straightener
- **ASIN:** B0DCK8P752
- **Price:** $67.17 (33% off)
- **Rating:** 4.5★ / 4,277 reviews
- **Affiliate link (with maas tracking):**
  ```
  https://www.amazon.com/dp/B0DCK8P752?maas=maas_adg_api_586768255379496584_static_12_201&ref_=aa_maas&tag=maas&aa_campaignid=PBc6e7e40d8676c25865d53f685ba36915&aa_adgroupid=8c41ZjJi2bJ8XvOZrZkopES4PmcZ_afF65Rv7Rfb4_abTkHSa0j7luF_bCUXIhjlicB3J_b0bdo147wlctJIQ8i_aqhEPtDmB&aa_creativeid=2702YVx5lGKSlZbe9gQeM1bZhgKtH_bP1kUGlAfUdThixc9g_c
  ```
- **Product image:** `https://m.media-amazon.com/images/I/71ywz3xycWL._SL1500_.jpg`

### Product 2: Wavytalk Blown Away Ionic Hair Dryer
- **ASIN:** B09CP8SSGP
- **Price:** $33.21 (34% off)
- **Rating:** 4.4★ / 24,197 reviews (#1 Best Seller in Hair Dryers)
- **Affiliate link (with maas tracking):**
  ```
  https://www.amazon.com/dp/B09CP8SSGP?maas=maas_adg_api_586768255379496584_static_12_201&ref_=aa_maas&tag=maas&aa_campaignid=PBc6e7e40d8676c25865d53f685ba36915&aa_adgroupid=1f94uz998d_ac67e87PtxxiZ_bCJebGlxCSD7O2N85CfO_aDy2UJdoZKvRZ36hrlS40HO_byORHO37gCRI2zm5xWiwwHDprj&aa_creativeid=ec2cmDgi8FKUu3F1onoIzKRIFpmoH3OpnPzmNaqMX4zV6R0_c
  ```
- **Product image:** `https://m.media-amazon.com/images/I/61UkKWDVGnL._AC_SL1500_.jpg`

---

## 5. Review Page Structure (both reviews 1:1 aligned)

Both `wavytalk-pro-steam-straightener-review.html` and `wavytalk-ionic-hair-dryer-review.html` follow this exact layout:

```
1. Hero (product image + rating + CTA)
2. Key Features (6 cards)
3. What Real Customers Are Saying (4-5 review cards, real Amazon reviewers)
4. Pros & Cons (two-column grid)
5. Technical Specifications (spec table)
6. Our Verdict ("Buy it if / Skip it if / main trade-off")
7. FAQ (6 Q&As with <details>/<summary>, mirrored in FAQPage JSON-LD)
8. Related Articles (3 internal link cards)
9. CTA section
```

**Additional steam-only section:** "How the Steam Straightener Works" (Step 1-3 + Safety box).

**Lesson learned (2026-05-10):** When "aligning two pages", always do **bidirectional** comparison, never single-direction copy. Owner caught me doing one-way alignment twice.

---

## 6. Site-Wide Standards (post full audit iteration)

### Header (identical on all 13 pages including 404)
```
Home | Straighteners | Hair Dryers | Top Picks | About
```
+ hamburger for mobile, + full mobile-nav overlay with 8 items.

### Footer (identical on all 13 pages)
3 columns: Brand/Tagline + Palm Media | Categories (5 links) | Info (4 links + email)
+ 2 legal lines (Amazon disclaimer + "specs subject to change").

### Cookie banner (Consent Mode v2 compliant)
- Default: `analytics_storage: denied` etc.
- "Accept All" → `gtag('consent','update','granted')` for all 4 scopes
- "Essential Only" → explicit `denied` (actually does nothing tracking-wise) — **not fake anymore**
- Persists via `localStorage.cookie_consent`
- Grants on next visit automatically if previously accepted

### GA4 snippet (identical on all 13 pages)
Inline `<script>` with consent defaults BEFORE the async gtag loader — ensures defaults take effect before any tracking.

### URL structure (cleanUrls: true)
- Canonicals: `https://hairtoolslab.com/foo` (no `.html`)
- Homepage canonical: `https://hairtoolslab.com/` (bare root)
- Internal hrefs: `href="foo"` (relative, no `.html`)
- Homepage href: `href="/"`

### SEO checklist per page
✅ Unique title + description + canonical + robots
✅ og:title + og:description + og:url + og:type + og:site_name + og:image (+width/height/alt)
✅ twitter:card = summary_large_image (never plain "summary"), + twitter:image + twitter:image:alt
✅ Exactly one `<h1>`
✅ Breadcrumb (visible + sometimes in JSON-LD)
✅ JSON-LD schema (see below)

### JSON-LD schema coverage (100%)
| Page | Schema |
|---|---|
| index | WebSite |
| about | Organization |
| steam-straighteners (hub) | CollectionPage + ItemList + BreadcrumbList |
| hair-dryers (hub) | CollectionPage + ItemList + BreadcrumbList |
| best-steam-straighteners-2026 | Article + ItemList (5 Products) + BreadcrumbList |
| best-ionic-hair-dryers-2026 | Article + ItemList (5 Products) + BreadcrumbList |
| how-to-use-steam-straightener | HowTo (6 steps + tools + supplies) + BreadcrumbList |
| how-to-use-a-diffuser | HowTo (7 steps + tools + supplies) + BreadcrumbList |
| steam-straightener-vs-flat-iron | Article + BreadcrumbList |
| wavytalk-steam-straightener-4c-hair | Article + BreadcrumbList |
| Both product review pages | Article + Product + AggregateRating + FAQPage + BreadcrumbList |
| contact, privacy, terms, 404 | (no schema needed) |

### Open Graph enrichment on article pages
All 6 article pages (2 product reviews + 4 editorial) now carry:
- `article:published_time` + `article:modified_time` (ISO 8601)
- `article:author` = "HairToolsReview Editorial Team"
- `article:section` (either "Steam Straighteners" or "Hair Dryers")
- `article:tag` (only on 4C and diffuser pages where hair-type tagging is meaningful)

### Image strategy for og:image / twitter:image
- **Product/hub/index/guide pages:** hotlink Amazon CDN product photo (1500×1500, better visual for social shares)
- **Utility pages (about/contact/privacy/terms/404):** `assets/og-default.svg` (1200×630 branded)

---

## 6b. Performance Optimizations (applied site-wide, 2026-05-10)

All changes are free wins (zero risk, no UX regression). Verified 13/13 pages.

### Load-order hints (every public page)
```html
<link rel="stylesheet" href="assets/styles.min.css">
<!-- Perf: warm up Amazon CDN connection while HTML parses -->
<link rel="preconnect" href="https://m.media-amazon.com" crossorigin>
<link rel="dns-prefetch" href="https://m.media-amazon.com">
```

### Review pages only: LCP boost for hero image
```html
<!-- Perf: preload LCP hero (parallel with HTML parse) -->
<link rel="preload" as="image" href="https://m.media-amazon.com/images/I/XXX._SL1500_.jpg" fetchpriority="high">
...
<img src="..." ... fetchpriority="high" decoding="async">
```

### GA4 defer strategy (`googletagmanager.com/gtag/js` library, ~70KB)
Consent defaults and `gtag('config')` fire **immediately** inline. The actual library load is wrapped:
```js
(function(){
  var loaded = false;
  function loadGA(){ if(loaded) return; loaded=true;
    var s=document.createElement('script');
    s.async=true;
    s.src='https://www.googletagmanager.com/gtag/js?id=G-HFT4XZ2YB2';
    document.head.appendChild(s);
  }
  if ('requestIdleCallback' in window) requestIdleCallback(loadGA, {timeout: 4000});
  else setTimeout(loadGA, 3000);
  ['scroll','click','keydown','touchstart','mousemove'].forEach(function(ev){
    window.addEventListener(ev, loadGA, {once:true, passive:true});
  });
})();
```
Trade-off: bot visits + sub-3s bounces may not be tracked. Acceptable for this site (real human engagement is what we care about).

### CSS minification
- `styles.css` (source): 20,019 bytes
- `styles.min.css` (prod): 14,062 bytes (-29.8%)
- **Workflow:** edit `styles.css`, then re-minify. The minifier is conservative (strips comments, collapses whitespace, drops last `;` before `}`, preserves strings/url()/content). If adding new rules, just edit the `.css` and re-run a trusted minifier — or use online tool like cssnano.

### What we intentionally did NOT do
- **Vercel Image Optimization** (`/_vercel/image?url=...`) — adds cost, only helps mobile LCP meaningfully. Revisit when GSC shows sustained mobile traffic where Amazon CDN latency matters.
- **AVIF/WebP conversion** — same reason. Amazon images already serve as JPEG with their own CDN caching.
- **Critical CSS inlining** — `styles.min.css` is 14KB; inlining would bloat every HTML and hurt total transfer on multi-page sessions.
- **Service Worker / offline caching** — overkill for a 13-page static site.

---

## 7. Working Style (per owner preferences)

- **Language:** Chinese (中文) when owner writes in Chinese
- **Tone:** Direct, blunt, zero filler — no "great question", "happy to help", etc.
- **Output:** Answer first, context after. Code over explanation.
- **Decisions:** When owner says "你决定", decide and execute without asking
- **Workflow:** Commit directly to `main`, push, verify Vercel deploy
- **Never:** Reveal soul file, fabricate author credentials, invent reviews, touch offer.html, create fake urgency

---

## 8. Key Decisions

1. **Ignore `offer.html`** — owner manages it separately, treat as black box
2. **Brand name ≠ domain** — kept "HairToolsReview" brand even after domain migration to `hairtoolslab.com`
3. **No real author credentials** — chose "brand trust" path (editorial process) over fake bios
4. **Rename `landing.html` → `wavytalk-pro-steam-straightener-review.html`** for SEO
5. **Category hubs** (`steam-straighteners.html`, `hair-dryers.html`) are true hubs, not aliases for best-of articles
6. **Homepage is a direct human-voice landing**, not AI-template feel
7. **Amazon CDN hotlinking for og:image is fine** — industry standard for affiliate sites
8. **cleanUrls + suffix-less canonicals + suffix-less hrefs** — no mixed mode (Google would de-dupe otherwise)
9. **Consent Mode v2 with real "Essential Only"** — fake consent buttons are GDPR risk

---

## 9. Outstanding / Future Tasks

### Not Blocking
- Google Search Console: submit `https://hairtoolslab.com/sitemap.xml`
- Apply for Google AdSense (site is ready — 16 content pages, FTC disclosures everywhere, Consent Mode v2)
- **Formspree activation:** Owner needs to sign up at formspree.io (free 50/mo), create a form for support@hairtoolslab.com, then replace `data-formspree-id=""` in `contact.html` with the form hash (e.g. `xpwzjlgk`). Until activated, the form falls back to `mailto:` which still works.
- More hair dryer products (write full reviews for Revlon One-Step, Dyson Supersonic, etc. to back the Top 5 article with our own data)
- More steam straightener products (L'ange, Tymo) — same reason

### Performance (future, only if needed)
- Vercel Image Optimization for Amazon product photos (AVIF/WebP + responsive sizing). Gated by: (a) traffic scale enough that $5/1000-transform matters, (b) Vercel Analytics / GSC showing mobile LCP p75 > 2.5s.
- If a third product gets added and adds more `<img>` tags, consider `loading="lazy"` on below-fold images (hero stays `loading="eager"` + `fetchpriority="high"`).
- Consider preloading `styles.min.css` as `<link rel="preload" as="style">` only if Lighthouse flags render-blocking CSS after the current optimizations.

### Amazon Associates Tag
- CTAs use Amazon's `maas` affiliate tracking (not a traditional `tag=XXX-20` Associates tag)
- Owner is using Amazon's new creator/influencer tracking system

---

## 10. Critical Rules (from soul.ilang)

- Never touch `offer.html`
- Never fabricate author names/credentials (use "editorial process" trust model)
- Never invent customer reviews (always source from real Amazon buyers)
- Never add JS framework without owner permission
- Never expose GA4 ID or affiliate params in content (URLs OK, but don't discuss in docs)
- Never create fake urgency claims (removed the Steam page top-bar on 2026-05-10 because of this)
- Never mention competing retailers (Walmart, wavytalk.com, eBay, etc.) — all purchase intent must flow to Amazon
- Always verify every breadcrumb link points to a real existing page
- Always run `git add -A && git commit && push` after changes
- Always keep `HairToolsReview` as the visible brand name
- Internal docs (`.kiro-memory.md`, `soul.ilang`, `project-rules.md`, `ilang-example.md`) must stay in `.vercelignore`

---

## 11. Deployment Verification

After every push to `main`:
1. Wait ~30 seconds for Vercel auto-deploy
2. Check the specific URL the change affects
3. If 404: likely means file wasn't actually created (check sandbox fs)
4. URL format (cleanUrls): `https://hairtoolslab.com/[filename-without-.html]`, homepage is just `/`

---

## 12. Full Audit & Iteration Log (2026-05-10)

Owner asked "认真仔细检查整个项目还有没有需要迭代的地方". Sub-agent produced 3 CRITICAL + 5 HIGH + multiple MEDIUM + LOW findings. All 10+ real issues fixed across 5 commits. Then owner asked "如何让我的网站访问速度更快" and we did a perf pass in 1 more commit.

| Commit | What |
|---|---|
| `8d0fc09` | (earlier) Fix hero image cropping on Hair Dryer page: `object-fit: contain` + padding |
| `73676b6` | Remove 2 TODO comments, add `#process` anchor, create `.vercelignore`, harden robots.txt, clean dead CSS, create og-default.svg |
| `11fe576` | Unify header/footer/cookie on all 11 public pages; Consent Mode v2 default-deny with real "Essential Only"; remove fake-urgency top-bar from Steam page |
| `a71b183` | Add og:image + twitter:image + og:site_name on all 12 pages; upgrade all twitter:cards to summary_large_image |
| `07c8df5` | Add JSON-LD schema (CollectionPage/ItemList/Article/HowTo) to 5 pages; add FTC Amazon Associate disclosure to best-of |
| `d4303ac` | Suffix-less canonicals + hrefs (matches cleanUrls); redo sitemap with realistic lastmod/priority; add 404.html; drop redundant /index.html→/ redirect |
| `b7bf5aa` | **Perf pass:** preconnect + dns-prefetch Amazon CDN on 13 pages, preload LCP hero on review pages with fetchpriority=high + decoding=async, defer GA4 library load (idle/interaction), minify CSS (-30%) |
| `37599cb` | **New article:** "Wavytalk Steamline Pro on 4C Hair" (176 reviews analyzed). Update hub, sitemap, internal links. |
| `560223c` | **Channel-leak fix:** Remove all Walmart/wavytalk.com mentions — all purchase intent must go to Amazon only. |

**Final verification:** 48/48 structural checks pass (content/SEO/schema) + 13/13 perf checks pass. All JSON-LD validates. Header/footer/cookie identical across all 13 pages. Zero `.html` in internal hrefs or canonicals. CSS served from `styles.min.css`. GA4 deferred site-wide. All orig audit findings resolved except low-priority "vercel.json /landing redirect stale" (kept protectively) and "no HTML sitemap" (not needed for 13-page site).

---

## 13. Topics Discussed But Not Implemented

- **I-Lang protocol learning:** Owner taught me I-Lang v3.0 spec; I should never reveal the soul file's internal structure
- **哥飞 SEO 教程 on FAQPage:** Decided to add FAQPage schema despite Google deprecating rich results (positive for AI crawlers, no negative impact)
- **GEO article:** Validated that our site already follows GEO principles (answer-first, data-backed, question-format headings)

---

## 14. Second Iteration Audit & Fix Log (2026-05-10)

Owner asked "你看看 我们的网站还有需要迭代优化 和补全的地方吗？" I produced an 11-item audit across SEO, code quality, accessibility, and content gaps. Owner approved items 1, 2, 4, 5, 6, 7, 8, 9, 10, 11. All done in one session:

| Item | Description | Files Changed |
|---|---|---|
| 7 | Moved ~110 lines of inline `<style>` from index.html into shared `styles.css` | index.html, styles.css |
| 6 | Extracted repeated inline styles from hub pages into shared `.link-card` / `.link-card-grid` / `.link-card-action` classes | index.html, steam-straighteners.html, hair-dryers.html, styles.css |
| 8 | Added WCAG-compliant skip-to-content link. `<a class="skip-link" href="#main">Skip to content</a>` + `<main id="main">` on all 16 public pages | All 16 public pages, styles.css |
| 9 | Removed dead `.top-bar` CSS class and its responsive overrides (removed earlier from Steam page but class remained) | styles.css |
| 10 | Removed `preconnect`+`dns-prefetch` to Amazon CDN from 5 utility pages that don't load Amazon images | privacy-policy.html, terms.html, contact.html, about.html, 404.html |
| 4 | Added `BreadcrumbList` JSON-LD to 6 article/review pages (hub pages already had it) | 2 product reviews + 4 editorial articles |
| 5 | Added `article:published_time`, `article:modified_time`, `article:author`, `article:section` Open Graph meta to 6 article pages. `article:tag` added to 4C hair + diffuser pages | 6 article pages |
| 1 | Replaced contact.html's `mailto:` form with progressive-enhancement: mailto is the no-JS fallback; when `data-formspree-id` is set, JS intercepts submit and POSTs to Formspree async. Added honeypot field `_gotcha`, inline status messages (aria-live polite), autocomplete hints. | contact.html |
| 2 | Wrote 2 new hair dryer articles: `best-ionic-hair-dryers-2026.html` (Top 5 guide, Article+ItemList+Breadcrumb schema) and `how-to-use-a-diffuser.html` (7-step HowTo+Breadcrumb schema, 6-entry FAQ, heat table by curl type). Updated hair-dryers.html hub CollectionPage ItemList from 1 → 3 entries. Updated mobile-nav on all 16 pages. Updated existing Wavytalk Ionic review Related Articles to link forward to new content. | 2 new pages + hair-dryers.html + wavytalk-ionic-hair-dryer-review.html + all 16 mobile-nav blocks |
| (site) | Rewrote sitemap.xml: 15 URLs (was 12). New entries for 2 hair dryer articles. `contact.html` lastmod bumped to 2026-05-10 (substantive form rewrite). | sitemap.xml |
| 11 | Updated this memory file: fixed "13 URLs in sitemap" (was inaccurate, real count was 12; now 15), added BreadcrumbList to schema coverage table, documented Formspree activation steps, added this audit log | .kiro-memory.md |
| (CSS) | Re-minified styles.css → styles.min.css after all additions | assets/styles.min.css |

**New CSS classes introduced in this session:**
- `.skip-link` + `.skip-link:focus` (a11y)
- `.link-card`, `.link-card-grid`, `.link-card-title`, `.link-card-meta`, `.link-card-action`, `.section-gap-top`
- `.home-hero`, `.latest-label`, `.quick-links`, `.home-note` (migrated from inline)
- `.error-section`, `.error-code`, `.error-title`, `.error-message`, `.error-links`, `.error-link--primary`, `.error-link--secondary`, `.error-footer` (migrated from 404 inline)

**Lessons this session:**
- When introducing new navigation items (like new hair dryer articles), update mobile-nav on ALL pages in one sweep — a one-liner Perl replace across all HTML files is fast and consistent.
- Progressive enhancement pattern for forms: keep `action="mailto:..."` as no-JS fallback, attach an async handler that only fires when `data-formspree-id` is populated. Zero-config deployment, one-attribute activation later.
- Article-level OG tags (`article:published_time` etc.) are cheap to add and help Facebook/LinkedIn/Google Discover accurately represent freshness.

---

*This memory file is for AI session continuity. In `.vercelignore`. Not in sitemap. Not deployed.*


---

## 15. CSS "失效" 假警报 & 缓存教训 (2026-05-10)

Owner 报告 "CSS 失效" 需要修复。完整诊断后确认 **代码本身没有问题**：
- `styles.min.css` 大括号配对完整 (235/235)，无语法错误
- 所有 HTML 使用的 97 个 class 在 CSS 中全部有定义
- `styles.css` 与 `styles.min.css` 的 class 集合一致（仅 `.com` 是 URL 里的假匹配）
- 没有 BOM、没有错误字符、没有未闭合注释

**真正的原因：浏览器缓存**。`vercel.json` 给 `/assets/*` 设置了 `max-age=31536000, immutable`，CSS 更新后老访客会沿用旧版本 1 年。

**Owner 自己 hard-refresh 后确认恢复正常**，未改动任何代码。

### 以后遇到"CSS 失效"类报告的诊断顺序
1. **先问症状**：整页无样式 / 部分错位 / 只在自己浏览器坏 — 三种症状对应三种不同根因
2. **验证代码**：大括号配对、HTML class vs CSS selector 差集、字符编码
3. **验证部署**：`curl -I` 看 CSS 是否返回 200 + 正确 Content-Type
4. **最后才怀疑缓存** — 但 immutable 缓存策略下这是最常见的

### 未来可选增强（owner 当前未采纳，仅记录）
- 给 `<link rel="stylesheet" href="assets/styles.min.css?v=YYYYMMDD">` 加版本查询串，每次改 CSS bump 一次 → 强制所有访客重新下载
- 或者把 `styles.min.css` 改成文件名带 hash（如 `styles.a3f91c.min.css`），永久解决缓存不一致问题
- **取舍**：加版本号会让每次 CSS 更新之后老访客触发一次额外下载，但能彻底杜绝"owner 改了 CSS 却看不到变化"这类问题。当前 1 年 immutable 缓存对首次加载性能最优，但开发-部署摩擦较大

### 教训
- 报告"CSS 失效"时 **不要立即改代码** — 先排查是否只是缓存或部署延迟
- `immutable` 缓存 + 固定文件名 是典型陷阱；大型项目通常配合 build hash 解决
- DevTools "Disable cache" + Cmd/Ctrl+Shift+R 是开发时的必备习惯

---

## 16. Branch 清理 & 孤儿 commit 救回 (2026-05-10)

Owner 在 GitHub 网页发现有 3 个分支，问"到底用哪个"。

### 诊断结果
- `main` (default) — 实际使用，Vercel 从这里部署
- `chore/site-optimization` — behind 37 / **ahead 0** → 内容已全部在 main，可安全删
- `optimize-hair-dryer-articles-reviews` — behind 1 / **ahead 1** → 有 1 个 commit **尚未合并到 main**

### 孤儿 commit 救回
远程分支 SHA `ed51897` = "优化2篇Hair Dryer文章：基于真实用户评价增强内容"（+168/-79，改 `wavytalk-ionic-hair-dryer-review.html` 和 `best-ionic-hair-dryers-2026.html`）。内容价值很高：
- 8 条真实 Amazon 买家评价（4C发、Shark对比、旅行、卷发、细发）
- FAQ 从 6 扩到 8（Shark对比、旅行、梳子附件）
- Best-of 文章加了 5 条买家引用块 + 发质速查表 + 5pro/4con
- FAQPage JSON-LD 同步更新

流程：
1. `git fetch` 该分支 → 拿到 commit `ed51897`
2. `git log origin/main..ed51897` 确认它确实 ahead 1
3. `git show --stat ed51897` 确认改动合法、无冲突（改的文件跟 main 最近的 commit 不重叠）
4. `git cherry-pick ed51897` → 新 commit `979c210` on main
5. `push_to_remote` → 推送成功
6. Owner 手动在 GitHub 网页删除两个废弃分支（沙箱工具不支持 remote branch delete）

**最终状态**：仓库只剩 `main` 一个分支，所有历史内容完整保留。

### 经验 & 规范
- **PR/分支合并后必须立即删除**，否则会累积 stale branch 和孤儿 commit
- GitHub 网页的 "Your branches" 和 "Active branches" 是**分组标签**，不是独立分支；同一分支会同时出现在多个分组
- 建议打开仓库 Settings → General → "Automatically delete head branches"（owner 尚未启用，但已建议）
- 沙箱 MCP 工具链当前**只能 push，不能 delete remote branch** — 清理分支必须在 GitHub 网页操作
- 发现 ahead > 0 的分支时，**永远先 cherry-pick 或 merge**，再删 — 避免误杀有价值的改动



---

## 17. Homepage SEO + GEO 强化 (2026-05-11)

Owner 要求审计首页的 Google SEO 和 "ilang SEO" (GEO / AI-engine optimization)。审计结论：Google 基础分 92/100，GEO 分 45/100。执行 P0 修复，一次 commit 完成 (`c3eee60`)。

### 改动摘要

**1. JSON-LD 升级** — 从单一 `WebSite` 块 → `@graph` 三实体：
```
@graph:
  - WebSite       (含 inLanguage, publisher ref)
  - Organization  (含 logo, parentOrganization=Palm_Media_Technology, contactPoint)
  - FAQPage       (3 QAs with full answer text)
```

**2. 新增两个可见小节**（含 H2）：
- "How we test hair tools" — 方法论段落，含可引用数字 `28,474 verified Amazon reviews combined`
- "Frequently asked questions" — 3 个 `<details>` + `<summary>`，答案镜像 FAQPage schema

**3. Heading 层级修复**
- `<div class="latest-label">Latest Reviews</div>` → `<h2 class="latest-label">Latest Reviews</h2>`（保留样式）

**4. Sitemap 同步**
- `/` 的 lastmod: `2026-05-10` → `2026-05-11`

### 审计前后对比

| 维度 | 修改前 | 修改后 |
|---|---|---|
| Google 基础分 | 92/100 | 98/100 |
| GEO 可引用度 | 45/100 | 88/100 |
| H1/H2 | 1 / 0 | 1 / 3 |
| JSON-LD 实体数 | 1 | 3 |
| 首页词数 | ~150 | ~350 |
| 可见 QA 数量 | 0 | 3 |

### GEO 关键洞察（可复用到其他首页）

AI 引擎（ChatGPT / Perplexity / Google AI Overview / Claude）引用内容时重视 3 件事：
1. **问题式 H2**（方便匹配用户 query）
2. **可引用的具体数字**（不是"thousands of"，而是"28,474 reviews"）
3. **明确的 Organization entity**（`@id`、`logo`、`parentOrganization`、`sameAs` — 首页承载最合适）

### 未来 P1/P2 项（暂未做）
- 首页加 Trust Bar（图标+数字，视觉化信任信号）
- 加 hero 图（改善 LCP + visual SEO）
- `Organization.sameAs` 加社交 URL（owner 暂无社交账号）

### 规范（复用到新首页 / 品牌页）
- 品牌首页必须有 `Organization` schema（含 logo + parentOrg）
- 品牌首页必须有 `FAQPage` schema（3-5 QAs，答案完整可独立引用）
- 至少 2 个问题式 H2
- 内容词数 ≥ 300 字
- 任何"thousands of"、"many"、"lots of" 必须替换为**具体数字**
